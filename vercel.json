{
  "builds": [
    {
      "use": "@vercel/static-build",
      "src": "package.json",
      "config": {
        "zeroConfig": true,
        "framework": "nuxtjs",
        "buildCommand": "npm generate",
        "outputDirectory": "dist/"
      }
    }
  ]
}
